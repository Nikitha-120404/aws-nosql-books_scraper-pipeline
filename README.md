# aws-nosql-books_scraper-pipeline
AWS NoSQL data pipeline that scrapes book data from the web, stores raw data in Amazon DocumentDB, transforms it, and loads structured records into Amazon DynamoDB using Python and AWS SDK.
